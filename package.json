{
  "name": "scrollbrake",
  "version": "1.1.0",
  "private": true,
  "description": "Cross-browser WebExtension build for ScrollBrake.",
  "scripts": {
    "build": "node scripts/build.mjs all",
    "build:chrome": "node scripts/build.mjs chrome",
    "build:firefox": "node scripts/build.mjs firefox",
    "package": "node scripts/package.mjs all",
    "package:chrome": "node scripts/package.mjs chrome",
    "package:firefox": "node scripts/package.mjs firefox",
    "check": "node --check service-worker.js && node --check content-script.js && node --check popup.js && node --check ui-theme.js && node --check scripts/build.mjs && node --check scripts/package.mjs"
  }
}
